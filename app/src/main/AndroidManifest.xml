<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cse110team24.walkwalkrevolution">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"

        android:theme="@style/AppTheme"
        android:windowSoftInputMode="stateHidden">
        <activity android:name=".RouteDetailsActivity"></activity>
        <activity
            android:name=".SaveRouteActivity"
            android:label="@string/title_activity_saving_page"></activity>
        <activity
            android:name=".RoutesActivity"
            android:label="@string/title_activity_routes"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MockActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>